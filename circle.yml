version: 2
jobs:
    build:
        docker:
            - image: circleci/node:latest-browsers
        steps:
            - checkout
            - run: sudo npm install -g
            - run:
                command: mocha
                environment:
                  MOCHA_FILE: ./node_modules/.bin/mocha
                when: always
            - store_test_results:
                path: ./test_result
            - store_artifacts:
                path: ./test_result
